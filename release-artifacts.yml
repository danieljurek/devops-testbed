parameters: 
  - name: Artifacts
    type: object

extends:

steps:
  - pwsh: |
      '${{ convertFromJson(parameters.Artifacts) }}' | ConvertFrom-Json | ConvertTo-Json -Depth 100 | Write-Host
    displayName: Artifacts as JSON

  